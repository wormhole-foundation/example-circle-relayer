apiVersion: v1
kind: Service
metadata:
  name: circle-relayer
  namespace: circle-relayer
  annotations:
    prometheus.io/scrape: "true"
spec:
  selector:
    app: circle-relayer
  ports:
    - name: api
      protocol: TCP
      port: 3000


